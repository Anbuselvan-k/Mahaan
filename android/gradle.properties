properties
# Java & Gradle performance tuning
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError

# Enable AndroidX & Jetifier
android.useAndroidX=true
android.enableJetifier=true

# ? Suppress source/target version 8 warning when using JDK 21+
android.javaCompile.suppressSourceTargetDeprecationWarning=true

# ? Enable Gradle configuration cache for faster builds
org.gradle.configuration-cache=true # Changed to true
android.suppressUnsupportedCompileSdk=35

# Removed duplicate org.gradle.jvmargs and properties line